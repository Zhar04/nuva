# Client-safe config only — this file ships inside build/web. No server secrets.
# (Server secrets like ANTHROPIC_API_KEY live in Railway Variables, never here.)

# ─── Backend API (Django on Railway) ──────────────────────
API_BASE_URL=https://nuva-production.up.railway.app

# ─── Video calls (Jitsi) ──────────────────────────────────
JITSI_DOMAIN=meet.ffmuc.net

# ─── Legacy Supabase (publishable/anon key — public by design, RLS-gated) ──
# Still read by the not-yet-removed db_service/auth_service legacy paths. The
# anon key is safe to ship (TECHNICAL_LETTER: "designed to be public"); it is
# NOT a server secret. Kept so the legacy bootstrap initializes cleanly.
SUPABASE_URL=https://zliydmqixefzdiknbwol.supabase.co
SUPABASE_ANON_KEY=sb_publishable_9skKhDL0DL5ZbLz4vBamlw_5jpS396B
